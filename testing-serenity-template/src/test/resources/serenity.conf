environments {
    default {
        webdriver.base.url=""
    }

    DEV {
        webdriver.base.url=""
    }
}

chrome_experimental_options {
    excludeSwitches = "enable-automation"
}